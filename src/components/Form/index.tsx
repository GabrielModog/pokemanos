import React from 'react';

import { Button, Container } from './styles';

const Form = () => {
  return (
    <Container>
      <h1>Formulário</h1>
      <Button type="button">BUSCAR</Button>
    </Container>
  );
};

export default Form;
